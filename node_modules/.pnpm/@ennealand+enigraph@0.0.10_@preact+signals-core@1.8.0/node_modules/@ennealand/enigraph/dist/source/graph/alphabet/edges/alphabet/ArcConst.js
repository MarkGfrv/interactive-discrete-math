import { jsx as _jsx, jsxs as _jsxs } from "preact/jsx-runtime";
import styles from '../edge.module.css';
export const ArcConst = ({ x1, y1, x2, y2 }) => {
    const rad = Math.atan2(y2 - y1, x2 - x1);
    x2 -= Math.cos(rad) * 10;
    y2 -= Math.sin(rad) * 10;
    return (_jsxs("g", { class: styles.edge, children: [_jsx("path", { "stroke-width": '7.5', d: `M ${x1} ${y1} L ${x2} ${y2}`, class: styles.stroke }), _jsx("path", { "stroke-width": '5', class: styles.fill, d: `M ${x1} ${y1} L ${x2} ${y2}`, "marker-end": 'url("#end-arrow-common")' })] }));
};
