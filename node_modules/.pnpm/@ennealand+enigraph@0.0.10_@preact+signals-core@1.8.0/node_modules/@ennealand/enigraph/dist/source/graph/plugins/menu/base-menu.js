import { jsx as _jsx } from "preact/jsx-runtime";
import style from './menu.module.css';
export const BaseMenu = (props) => {
    if (!props.buttons.length)
        return null;
    return (_jsx("div", { class: `${style.menu} ${props.shown ? '' : style.hidden}`, style: { transform: `translate(${props.x}px, ${props.y}px) translate(-50%, 18px)` }, onMouseLeave: props.leave, children: props.buttons.map((button, index) => (_jsx("div", { class: style.button, onClick: e => button.action(e, props.x, props.y), children: button.content }, index))) }));
};
