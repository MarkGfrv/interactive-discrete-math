import { JSX } from 'preact/jsx-runtime';
export type MenuButton = {
    content: JSX.Element | undefined;
    action: (e: MouseEvent, x: number, y: number) => void;
};
type Props = {
    x: number;
    y: number;
    buttons: MenuButton[];
    leave: () => void;
    shown: boolean;
    padding: number;
};
export declare const BaseMenu: (props: Props) => JSX.Element | null;
export {};
