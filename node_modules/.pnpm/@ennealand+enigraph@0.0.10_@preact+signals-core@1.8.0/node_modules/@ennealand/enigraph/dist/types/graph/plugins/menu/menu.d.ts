import { INode } from '$lib/types';
import { ReadonlySignal } from '@preact/signals';
import { MenuButton } from './base-menu';
import { DeepSignal } from 'deepsignal';
type Props = {
    nodes: DeepSignal<INode[]>;
    selection: ReadonlySignal<Set<number>>;
    visible: ReadonlySignal<boolean>;
    buttons: ReadonlySignal<MenuButton[]>;
    padding?: number;
};
export declare const withMenu: (props: Props) => {
    Menu: () => import("preact").JSX.Element | null;
    hideMenu: () => void;
    menuNodePosition: ReadonlySignal<{
        x: number;
        y: number;
    } | null>;
};
export { type MenuButton };
