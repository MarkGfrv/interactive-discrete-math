import { EdgeType } from '$lib/types';
import { JSX } from 'preact/jsx-runtime';
interface Props {
    type: EdgeType;
    x1: number;
    y1: number;
    x2: number;
    y2: number;
    mousedown?: (e: JSX.TargetedMouseEvent<SVGGElement>) => void;
    mouseup?: (e: JSX.TargetedMouseEvent<SVGGElement>) => void;
    highlight?: boolean;
    padding?: number;
    noselect?: boolean;
}
export declare const Edge: ({ type, noselect, x1, y1, x2, y2, mousedown, mouseup, highlight, padding, }: Props) => JSX.Element;
export {};
