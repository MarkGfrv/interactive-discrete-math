import { NodeType } from '$lib/types';
import type { JSX } from 'preact/jsx-runtime';
interface Props {
    type: NodeType;
    x: number;
    y: number;
    label?: string;
    mousedown?: (e: JSX.TargetedMouseEvent<SVGGElement>) => void;
    mouseup?: (e: JSX.TargetedMouseEvent<SVGGElement>) => void;
    textDoubleClick?: () => void;
    highlight?: boolean;
    padding?: number;
    noselect?: boolean;
}
export declare const Node: ({ type, noselect, x, y, label, mousedown, mouseup, textDoubleClick, highlight, padding, }: Props) => JSX.Element;
export {};
