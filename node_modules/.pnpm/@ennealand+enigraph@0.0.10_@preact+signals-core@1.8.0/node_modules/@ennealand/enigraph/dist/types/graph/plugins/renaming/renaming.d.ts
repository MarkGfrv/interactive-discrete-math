import { INode } from '$lib/types';
import { DeepSignal } from 'deepsignal';
import { JSX } from 'preact/jsx-runtime';
type Props = {
    submit?: (element: INode, newValue: string) => void;
};
export declare const withRenaming: (props: Props) => {
    RenamingArea: () => JSX.Element | null;
    startRenaming: (node: DeepSignal<INode>) => void;
    stopRenaming: () => void;
    isRenaming: import("@preact/signals-core").Signal<{
        node: DeepSignal<INode>;
    } | null>;
};
export declare const RenamingArea: (props: {
    x: number;
    y: number;
    value: string;
    submit: (newValue: string) => void;
}) => JSX.Element;
export {};
