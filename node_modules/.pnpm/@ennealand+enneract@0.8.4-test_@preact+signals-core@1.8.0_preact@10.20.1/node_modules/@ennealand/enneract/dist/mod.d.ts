import { EnneractEngine, Options } from './engine.ts';
import { Magic } from './types/magic-types.ts';
export declare class Enneract<App> {
    readonly engine: EnneractEngine;
    private readonly magicModule;
    constructor(url: string, options?: Options);
    get magic(): Magic<App>;
    $disconnect(): Promise<void>;
    [Symbol.dispose](): Promise<void>;
    [Symbol.asyncDispose](): Promise<void>;
}
export { Signal, batch, computed, effect, signal, untracked, type ReadonlySignal } from '@preact/signals';
export { deepSignal } from 'deepsignal';
export { $and } from './magic/and.ts';
export type { Magic, Query } from './types/magic-types.ts';
export { ScType } from './types/sc-type.ts';
export { smartBatch } from './utils/smart-reactive.ts';
